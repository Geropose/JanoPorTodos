version: "3"

services:
  php:
    image: porchn/php5.6-apache
    volumes:
      - .:/var/www/html
    ports:
      - "80:80"
    environment:
      - TZ=America/Buenos_Aires
  mailhog:
    image: mailhog/mailhog
    ports:
      - "8025:8025"
